# Canonical workflow: .github/workflows/attestack-evidence.yml
#
# This repo dogfoods evidence capture via CI. To reuse in another project:
#
#   evidence:
#     uses: kiket-dev/attestack/.github/workflows/attestack-evidence.yml@main
#
# Or copy .github/workflows/attestack-evidence.yml from this repository.

name: Attestack CI evidence (see .github/workflows/attestack-evidence.yml)

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use .github/workflows/attestack-evidence.yml in this repository."
